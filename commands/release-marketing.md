Generate marketing content from recent code changes pushed to production.

Invoke the `release-marketing` skill with arguments: $ARGUMENTS

If no arguments provided, default to analyzing recent changes and asking what content type to generate (blog / landing-page / social / changelog / all).
