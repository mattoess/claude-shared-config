Send a rendered RGA promo video to GoHighLevel as platform-specific drafts.

Invoke the `promo-video` skill in post mode with arguments: $ARGUMENTS

If no arguments are provided, list recent Promo Briefs with status `rendered` and ask the user to pick one.

Detailed instructions live in `skills/promo-video/SKILL-post.md`.
